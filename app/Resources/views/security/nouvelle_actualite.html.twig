{% extends 'neoxiBase.html.twig' %}

{% block body %}
    <div class="container-fluid globalContainer">
    <div class="row">
        <h1 class="col-xs-11 col-xs-offset-1">Nouvelle actualité</h1>
    </div>
        <div class="row">
            <div class="col-xs-10 col-xs-offset-1">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" value="Create" />
                {{ form_end(form) }}
                    <div class="row">
                        {% set id = 1 %}
                        {% for imgNumber in 1..12%}

                            <img class="col-xs-2 imgBack" src="{{ asset ('images/banque/image_actu' ~ id ~ '.jpg') }}">

                            {% set id = id + 1 %}
                        {% endfor %}
                    </div>
                <ul>
                    <li>
                        <a href="{{ path('admin') }}">Retour à l'accueil</a>
                    </li>
                </ul>
            </div>
        </div>
        <div class="container marginContainer"></div>
    </div>
{% endblock %}